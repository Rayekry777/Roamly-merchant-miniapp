import { listStaff, inviteStaff, disableStaff, activateStaff, type MerchantStaff } from "../../api/merchant-staff";
Page({data:{staff:[] as MerchantStaff[],loading:true,error:"",phone:"",role:"MANAGER" as "MANAGER"|"VERIFIER"},onShow(){void this.load()},async load(){this.setData({loading:true});try{const r=await listStaff();this.setData({staff:r.data?.items||[],error:""});}catch(e){this.setData({error:e instanceof Error?e.message:"加载失败"});}finally{this.setData({loading:false});}},onPhone(e:WechatMiniprogram.Input){this.setData({phone:e.detail.value});},onRole(e:WechatMiniprogram.PickerChange){this.setData({role:String(e.detail.value)==="0"?"MANAGER":"VERIFIER"});},async invite(){if(!this.data.phone)return wx.showToast({title:"请输入手机号",icon:"none"});try{const r=await inviteStaff(this.data.phone,this.data.role);wx.showModal({title:"邀请已生成",content:`邀请码：${r.data?.token||"已发送"}`});this.setData({phone:""});}catch(e){wx.showToast({title:e instanceof Error?e.message:"邀请失败",icon:"none"});}},async toggle(e:WechatMiniprogram.TouchEvent){const id=String(e.currentTarget.dataset.id);const item=this.data.staff.find(v=>v.id===id);if(!item)return;try{if(item.status==="ACTIVE")await disableStaff(id);else await activateStaff(id);await this.load();}catch(err){wx.showToast({title:err instanceof Error?err.message:"操作失败",icon:"none"});}}});
